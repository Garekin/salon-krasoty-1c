#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ)
	Если ЗначениеЗаполнено(Номер) И ЗначениеЗаполнено(ДатаНачала) Тогда
		Наименование = СтрШаблон("№%1 от %2 г.", Номер, Формат(ДатаНачала, "ДФ=dd.MM.yyyy"));
	КонецЕсли;
КонецПроцедуры

#КонецОбласти
